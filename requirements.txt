# Core scientific stack
numpy
pandas
h5py
openpyxl          # Excel metadata reading

# Allen Institute ephys libraries
ipfx
neuroanalysis

# Required by bundled aisynphys/config.py
PyYAML

# Bundled local library (install after the above)
# cd aisynphys && pip install -e . && cd ..

# Dimensionality reduction & ML
umap-learn
scikit-learn
matplotlib
